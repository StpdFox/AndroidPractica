<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.surfacepro3.week3">
    <provider
        android:name="com.example.surfacepro3.CalendarProvider"
        android:authorities="com.example.surfacepro3.CalendarProvider" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Holo.Light">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Database.DataListActivity$NewContactActivity"
            android:theme="@android:style/Theme.Holo.Light.Dialog" />
        <activity android:name=".Database.DataListActivity"

            />

<activity android:name=".extendedcalendarview.CalendarFragment"
        android:theme="@android:style/Theme.Holo.Light.Dialog"/>

        <activity android:name=".Database.SearchContactActivity" />

        <activity
            android:name=".extendedcalendarview.CalendarActivity"
            >
        </activity>
    </application>

</manifest>